export default class Position{
    compartment;
    shelf;
    x;
    y;

    constructor(compartment, shelf, x, y){
        this.compartment = compartment;
        this.shelf = shelf;
        this.x = x;
        this.y = y;
    }
}